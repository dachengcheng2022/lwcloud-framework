server:
  port: 10763
spring:
  jackson:
    time-zone: GMT+8
  application:
    name: gateway-server
    profiles:
      active: dev
  cloud:
    config:
      username: user
      password: tZzJxR7SasBxi8LB
      name: application-gateway
      profile: dev
      uri: http://localhost:10762/
      label: develop
      discovery:
        serviceId: gateway-server